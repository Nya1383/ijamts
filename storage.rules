rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all files
    match /submissions/{fileName} {
      allow read: if true;
      // Allow public write access for file uploads (can be limited to authenticated users)
      allow write: if true;
    }
  }
} 